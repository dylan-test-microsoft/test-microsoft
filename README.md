# test-microsoft

A simple standalone login page with a fixed, accessible login button.

## Run locally

Open `index.html` in a browser, or serve this directory with any static file
server.

## Test

Run the dependency-free regression tests with:

```sh
node --test script.test.js
```
