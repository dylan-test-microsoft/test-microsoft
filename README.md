# Test Microsoft

This repository currently contains a minimal project scaffold for validating
GitHub and Cursor Cloud agent workflows.

## Repository status

The project only includes documentation at this stage. There is no application
source code, dependency manifest, build configuration, or automated test suite
checked in yet.

## Getting started

Clone the repository and inspect the project root:

```bash
git clone https://github.com/dylan-test-microsoft/test-microsoft.git
cd test-microsoft
git status
```

No dependency installation is required for the current repository contents.

## Testing

No automated test suite has been added yet. For documentation-only changes, run:

```bash
git diff --check
```
